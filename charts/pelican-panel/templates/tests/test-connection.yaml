apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pelican-panel.fullname" . }}-test-connection"
  labels:
    {{- include "pelican-panel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pelican-panel.fullname" . }}:{{ .Values.service.port }}/up']
  restartPolicy: Never
